# 模形式與模擬模形式文檔

這是一個關於模形式與模擬模形式理論的文檔網站，使用 GitHub Pages 部署。

## 📚 文檔內容

- **主速查表** (`mmf_sheet.md`): 核心概念的快速參考
- **補充概念速查表** (`supplement_sheet.md`): 詳細的概念解釋

## 🚀 部署到 GitHub Pages

### 方法 1: 自動部署（推薦）

1. 確保所有文件已提交到 GitHub 倉庫
2. 前往 GitHub 倉庫的 **Settings** → **Pages**
3. 在 **Source** 部分選擇 **Deploy from a branch**
4. 選擇分支：`main`（或 `master`）
5. 選擇文件夾：`/ (root)`
6. 點擊 **Save**

GitHub 會自動使用 Jekyll 構建網站，幾分鐘後即可在以下網址訪問：
```
https://[你的用戶名].github.io/MMF_note
```

### 方法 2: 本地測試

如果你想在本地測試網站：

1. 安裝 Ruby 和 Bundler：
   ```bash
   gem install bundler
   ```

2. 安裝依賴：
   ```bash
   bundle install
   ```

3. 啟動本地服務器：
   ```bash
   bundle exec jekyll serve
   ```

4. 在瀏覽器中打開 `http://localhost:4000/MMF_note`

## 📁 文件結構

```
.
├── _config.yml          # Jekyll 配置文件
├── index.md             # 首頁
├── mmf_sheet.md         # 主速查表
├── supplement_sheet.md  # 補充概念速查表
├── Gemfile              # Ruby 依賴
├── .gitignore          # Git 忽略文件
└── README.md           # 本文件
```

## 🔧 配置說明

- `_config.yml`: 包含網站標題、描述、URL 等配置
- `baseurl`: 設置為 `/MMF_note`（與倉庫名稱一致）
- 使用 `minima` 主題（GitHub Pages 默認主題）
- 支持 MathJax 數學公式渲染

## 📝 更新文檔

1. 編輯 Markdown 文件
2. 提交更改：
   ```bash
   git add .
   git commit -m "更新文檔"
   git push
   ```
3. GitHub Pages 會自動重新構建網站

## 🔗 相關連結

- [GitHub Pages 文檔](https://docs.github.com/en/pages)
- [Jekyll 文檔](https://jekyllrb.com/docs/)

